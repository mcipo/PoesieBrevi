package boundary;

import javax.swing.*;
import java.awt.*;

public class StatsTagPanel extends JPanel {
    public StatsTagPanel() {
        setLayout(new BorderLayout());
        setBorder(BorderFactory.createEmptyBorder(15, 15, 15, 15));
        setBackground(Color.WHITE);
    }
}
